<section class="furnace-cta">
  <div class="cta-overlay"></div>

  <div class="container cta-content">

    <span class="overline">
      NO HEAT RIGHT NOW?
    </span>

    <h2>
      Tell us what your furnace is doing
      <span>and we will tell you what it means.</span>
    </h2>

    <p>
      Most no heat calls in Bellingham get a same day visit.
      Call and describe the symptoms, even if you are not sure
      how to explain them.
    </p>

    <div class="cta-buttons">

      <a href="tel:+13605550143" class="btn btn-primary">
        <svg
          class="btn-icon"
          viewBox="0 0 24 24"
          fill="none"
          aria-hidden="true"
        >
          <path
            d="M22 16.9v3a2 2 0 0 1-2.2 2
               19.8 19.8 0 0 1-8.6-3.1
               19.5 19.5 0 0 1-6-6
               19.8 19.8 0 0 1-3.1-8.7
               A2 2 0 0 1 4.1 2h3
               a2 2 0 0 1 2 1.7
               c.1 1 .4 2 .7 2.9
               a2 2 0 0 1-.5 2.1L8 10
               a16 16 0 0 0 6 6l1.3-1.3
               a2 2 0 0 1 2.1-.5
               c.9.3 1.9.6 2.9.7
               a2 2 0 0 1 1.7 2Z"
          />
        </svg>

        Call (360) 555-0143
      </a>

      <a href="#contact" class="btn btn-secondary">
        Request a Visit
      </a>

    </div>

  </div>
</section>

<style>
.furnace-cta {
  position: relative;
  padding: 105px 24px;
  overflow: hidden;
  background:
    linear-gradient(
      135deg,
      #0b1f3a 0%,
      #102b50 55%,
      #173b68 100%
    );
}

.furnace-cta::before {
  content: "";
  position: absolute;
  top: -150px;
  right: -100px;
  width: 420px;
  height: 420px;
  border-radius: 50%;
  background: rgba(249, 115, 22, 0.16);
  filter: blur(10px);
}

.furnace-cta::after {
  content: "";
  position: absolute;
  bottom: -190px;
  left: -140px;
  width: 420px;
  height: 420px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.06);
}

.cta-overlay {
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(
      rgba(255, 255, 255, 0.025) 1px,
      transparent 1px
    ),
    linear-gradient(
      90deg,
      rgba(255, 255, 255, 0.025) 1px,
      transparent 1px
    );
  background-size: 42px 42px;
  pointer-events: none;
}

.container {
  width: min(1000px, 100%);
  margin: 0 auto;
}

.cta-content {
  position: relative;
  z-index: 2;
  text-align: center;
}

.overline {
  display: inline-block;
  margin-bottom: 20px;
  color: #fb923c;
  font-size: 0.8rem;
  font-weight: 800;
  letter-spacing: 2.5px;
}

.overline::after {
  content: "";
  display: block;
  width: 65px;
  height: 3px;
  margin: 11px auto 0;
  border-radius: 999px;
  background: #f97316;
}

.furnace-cta h2 {
  max-width: 860px;
  margin: 0 auto 24px;
  color: #ffffff;
  font-size: clamp(2.25rem, 5vw, 3.75rem);
  font-weight: 850;
  line-height: 1.08;
  letter-spacing: -1.6px;
}

.furnace-cta h2 span {
  display: block;
  margin-top: 7px;
}

.furnace-cta p {
  max-width: 720px;
  margin: 0 auto 38px;
  color: #d5deea;
  font-size: 1.1rem;
  line-height: 1.8;
}

.cta-buttons {
  display: flex;
  justify-content: center;
  gap: 18px;
  flex-wrap: wrap;
}

.btn {
  display: inline-flex;
  min-height: 58px;
  align-items: center;
  justify-content: center;
  gap: 11px;
  padding: 16px 30px;
  border: 2px solid transparent;
  border-radius: 12px;
  text-decoration: none;
  font-weight: 800;
  transition:
    transform 0.3s ease,
    background 0.3s ease,
    border-color 0.3s ease,
    color 0.3s ease,
    box-shadow 0.3s ease;
}

.btn-primary {
  color: #ffffff;
  background: #f97316;
  box-shadow:
    0 15px 36px rgba(249, 115, 22, 0.3),
    0 0 20px rgba(249, 115, 22, 0.14);
}

.btn-primary:hover {
  transform: translateY(-5px);
  background: #ea580c;
  box-shadow:
    0 22px 45px rgba(249, 115, 22, 0.38),
    0 0 28px rgba(249, 115, 22, 0.22);
}

.btn-secondary {
  color: #ffffff;
  border-color: rgba(255, 255, 255, 0.7);
  background: rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(8px);
}

.btn-secondary:hover {
  transform: translateY(-5px);
  color: #0f172a;
  border-color: #ffffff;
  background: #ffffff;
  box-shadow: 0 18px 38px rgba(0, 0, 0, 0.18);
}

.btn-icon {
  width: 21px;
  height: 21px;
  overflow: visible;
  stroke: currentColor;
  stroke-width: 1.8;
  stroke-linecap: round;
  stroke-linejoin: round;
}

@media (max-width: 640px) {
  .furnace-cta {
    padding: 82px 18px;
  }

  .furnace-cta h2 {
    font-size: 2.25rem;
  }

  .furnace-cta p {
    font-size: 1rem;
  }

  .cta-buttons {
    flex-direction: column;
  }

  .btn {
    width: 100%;
  }
}

@media (prefers-reduced-motion: reduce) {
  .btn {
    transition: none;
  }

  .btn:hover {
    transform: none;
  }
}
</style>