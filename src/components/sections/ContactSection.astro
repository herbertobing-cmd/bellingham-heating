<section class="contact-section">
  <div class="container contact-grid">

    <aside class="contact-info">
      <span class="overline">GET IN TOUCH</span>
      <h2>Ready to Get Your Heat Back?</h2>
      <p>Call today or send us a message. We repair furnaces throughout Bellingham and Whatcom County.</p>

      <div class="info-card">
        <h3>Call Us</h3>
        <a href="tel:+13605550143">(360) 555-0143</a>
      </div>

      <div class="info-card">
        <h3>Service Area</h3>
        <p>Bellingham • Ferndale • Lynden • Blaine • Everson</p>
      </div>

      <div class="info-card">
        <h3>Business Hours</h3>
        <p>Mon–Fri<br>8:00 AM – 6:00 PM</p>
      </div>
    </aside>

    <div class="contact-form-wrap">
      <form class="contact-form">
        <div class="field">
          <label>Full Name</label>
          <input type="text" placeholder="John Smith">
        </div>

        <div class="field">
          <label>Phone Number</label>
          <input type="tel" placeholder="(360) 555-1234">
        </div>

        <div class="field">
          <label>Email Address</label>
          <input type="email" placeholder="john@email.com">
        </div>

        <div class="field">
          <label>What is your furnace doing?</label>
          <textarea rows="6" placeholder="Describe the problem..."></textarea>
        </div>

        <button type="submit">BOOK MY VISIT</button>
      </form>
    </div>

  </div>
</section>

<style>
.contact-section{
padding:110px 24px;
background:#f8fafc;
}
.container{
width:min(1180px,100%);
margin:auto;
}
.contact-grid{
display:grid;
grid-template-columns:420px 1fr;
gap:40px;
align-items:start;
}
.overline{
display:inline-block;
color:#f97316;
font-size:.8rem;
font-weight:800;
letter-spacing:2px;
margin-bottom:18px;
}
.contact-info h2{
font-size:clamp(2.2rem,4vw,3.3rem);
line-height:1.1;
margin:0 0 18px;
color:#0f172a;
}
.contact-info>p{
color:#64748b;
line-height:1.8;
margin-bottom:30px;
}
.info-card{
background:#fff;
padding:24px;
border:2px solid #e8edf3;
border-radius:18px;
margin-bottom:18px;
transition:.35s;
box-shadow:0 10px 28px rgba(15,23,42,.05);
}
.info-card:hover{
border-color:#f97316;
transform:translateY(-6px);
box-shadow:0 0 22px rgba(249,115,22,.16),0 18px 42px rgba(15,23,42,.12);
}
.info-card h3{
margin:0 0 10px;
color:#0f172a;
}
.info-card a{
color:#f97316;
font-weight:800;
text-decoration:none;
font-size:1.2rem;
}
.contact-form-wrap{
background:#fff;
border:2px solid #e8edf3;
border-radius:22px;
padding:34px;
box-shadow:0 15px 40px rgba(15,23,42,.06);
transition:.35s;
}
.contact-form-wrap:hover{
border-color:#f97316;
box-shadow:0 0 24px rgba(249,115,22,.15),0 22px 50px rgba(15,23,42,.12);
}
.contact-form{
display:grid;
gap:20px;
}
.field label{
display:block;
margin-bottom:8px;
font-weight:700;
color:#0f172a;
}
.field input,
.field textarea{
width:100%;
padding:16px;
border:2px solid #e5e7eb;
border-radius:12px;
font:inherit;
transition:.3s;
}
.field input:focus,
.field textarea:focus{
outline:none;
border-color:#f97316;
box-shadow:0 0 0 4px rgba(249,115,22,.12);
}
button{
padding:18px;
border:none;
border-radius:12px;
background:#f97316;
color:#fff;
font-weight:800;
cursor:pointer;
transition:.35s;
box-shadow:0 15px 35px rgba(249,115,22,.25);
}
button:hover{
background:#ea580c;
transform:translateY(-4px);
box-shadow:0 22px 45px rgba(249,115,22,.35);
}
@media(max-width:900px){
.contact-grid{grid-template-columns:1fr;}
}
</style>